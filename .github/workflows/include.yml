name: Builds Page

on:
  workflow_call:
    inputs:
      signing_policy:
        required: true
        type: string
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Print signing policy
        run: echo "Running for signing policy ${{ inputs.signing_policy }}"
        shell: bash
      - name: Download dummy executable
        run: curl -o random.dll https://github.com/carterjones/hello-world-dll/releases/download/v1.0.0/hello-world-x64.dll
      - name: upload-unsigned-artifact
        id: upload-unsigned-artifact
        uses: actions/upload-artifact@v4
        with:
          name: "upload-${{ inputs.signing_policy }}" 
          if-no-files-found: error
          path: random.dll
      - name: Submit Signing Request
        uses: signpath/github-action-submit-signing-request@v1
        with:
          api-token: '${{ secrets.SIGNPATH_API_TOKEN }}'
          organization-id: '${{ vars.SIGNPATH_ORGANIZATION_ID }}'
          project-slug: 'GitHub_Branch_Ruleset_Tests'
          signing-policy-slug:  '${{ input.signing_policy }}'
          github-artifact-id:  "${{steps.upload-unsigned-artifact.outputs.artifact-id}}"
          wait-for-completion: false
    
